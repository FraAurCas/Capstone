<div class="container">

    <!--
    <blockquote>
    Click on the event name for more details!
    Click on <i class="material-icons tiny">sort</i> to sort by that column, or items in <i
        class="material-icons tiny">filter_list</i> columns to filter by that item.
    </blockquote>
    <div class="row">
    <div class="col l3 s12 "> <i class="left material-icons">sort</i>Sorted by: Yes!</div>
    <div class="col l3 offset-l6 ">
        Position diffeently (left) for mobile
        <div class="right hide-on-small-only"><i class="left material-icons">filter_list</i>Filter: None</div>
        <div class="left hide-on-med-and-up"><i class="left material-icons">filter_list</i>Filter: None</div>
    </div>

    </div>
    -->

    <!-- https://materializecss.com/table.html -->

    <div>
        <!-- Searchbar -->
        <input type="text" placeholder="Search.." id="searchbox">
        <a class="waves-effect waves-light btn-small navbar"
            onclick="addUrlParameter('search', document.getElementById('searchbox').value)">Search</a>
    </div>

    <table class="highlight centered responsive-table">
        <thead>
            <th>ID</th>
            {{#if include_segment}}
                <th>Broking Segment</th>
            {{/if}}
            {{#if include_region}}
                <th>Broking Region</th>
            {{/if}}
            {{#if include_industry}}
                <th>Industry</th>
            {{/if}}
            {{#if include_hazardGroup}}
                <th>Hazard Group</th>
            {{/if}}
            {{#if include_revenue}}
                <th>Global Revenue</th>
            {{/if}}
            {{#if include_powerUnits}}
                <th>Power Units</th>
            {{/if}}
            {{#if include_insurableVaue}}
                <th>Total Insurable Value</th>
            {{/if}}
            {{#if include_payroll}}
                <th>Payroll</th>
            {{/if}}
            {{#if include_catastrophe}}
                <th>Catastrophe Exposure</th>
            {{/if}}
        </thead>
        <tbody>
            <tr>
                {{#each array}}
                <tr>
                    {{#each this}}
                    <td onclick="location.href='/account/{{this.id}}'">{{this}}</td>
                    {{/each}}
                </tr>
                {{/each}}
            </tr>
        </tbody>
    </table>
</div>

<div class="sidebar">

    <p></p>
    {{#if include_segment}}
        <a onclick="addUrlParameter('include_segment', 'false')">Including Segment</a>
    {{else}}
        <a onclick="addUrlParameter('include_segment', 'true')">Excluding Segment</a>
    {{/if}}

    <p></p>
    {{#if include_region}}
        <a onclick="addUrlParameter('include_region', 'false')">Including Region</a>
    {{else}}
        <a onclick="addUrlParameter('include_region', 'true')">Excluding Region</a>
    {{/if}}

    <p></p>
    {{#if include_industry}}
        <a onclick="addUrlParameter('include_industry', 'false')">Including Industry</a>
    {{else}}
        <a onclick="addUrlParameter('include_industry', 'true')">Excluding Industry</a>
    {{/if}}

    <p></p>
    {{#if include_hazardGroup}}
        <a onclick="addUrlParameter('include_hazardGroup', 'false')">Including Hazard Group</a>
    {{else}}
        <a onclick="addUrlParameter('include_hazardGroup', 'true')">Excluding Hazard Group</a>
    {{/if}}

    <p></p>
    {{#if include_revenue}}
    <a onclick="addUrlParameter('include_revenue', 'false')">Including Revenue</a>
    {{else}}
    <a onclick="addUrlParameter('include_revenue', 'true')">Excluding Revenue</a>
    {{/if}}

    <p></p>
    {{#if include_powerUnits}}
        <a onclick="addUrlParameter('include_powerUnits', 'false')">Including Power Units</a>
    {{else}}
        <a onclick="addUrlParameter('include_powerUnits', 'true')">Excluding Power Units</a>
    {{/if}}

    <p></p>
    {{#if include_insurableVaue}}
        <a onclick="addUrlParameter('include_insurableVaue', 'false')">Including Insurable Value</a>
    {{else}}
        <a onclick="addUrlParameter('include_insurableVaue', 'true')">Excluding Insurable Value</a>
    {{/if}}

    <p></p>
    {{#if include_payroll}}
        <a onclick="addUrlParameter('include_payroll', 'false')">Including Payroll</a>
    {{else}}
        <a onclick="addUrlParameter('include_payroll', 'true')">Excluding Payroll</a>
    {{/if}}

    <p></p>
    {{#if include_catastrophe}}
        <a onclick="addUrlParameter('include_retentions', 'false')">Including Catastrophe</a>
    {{else}}
        <a onclick="addUrlParameter('include_retentions', 'true')">Excluding Catastrophe</a>
    {{/if}}

</div>
<<<<<<< HEAD

<div class="row">

     <div class="button_wrapper col s2">
            <a class="submit waves-effect waves-light btn btn-large" type="submit" href="/adding_entries">Add Entries</a>
        </div>
</div>
=======
>>>>>>> 37be867b69b4937f7665dd772a3aae734b0dbd6d
